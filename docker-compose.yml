version: '3.8'

services:
  teldrive:
    build: .
    container_name: teldrive
    restart: always
    ports:
      - "8080:8080"
    environment:
      - TELDRIVE_SERVER_ADDRESS=0.0.0.0:8080
    volumes:
      - ./config.toml:/app/config.toml
